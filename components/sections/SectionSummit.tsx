export default function SectionSummit() {
  return (
    <section id="summit" className="reveal">
      <h3>Global Leadership Summit — Schedule & Agenda</h3>
      <div>{/* grid of sessions with image + text + time */}</div>
    </section>
  );
}
